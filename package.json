{
  "name": "youtube-article-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.mjs && npx wrangler dev --port 34997",
    "build": "node build.mjs",
    "deploy": "node build.mjs && npx wrangler deploy",
    "typecheck": "npx tsc --noEmit"
  },
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "marked": "^18.0.4",
    "youtubei.js": "^13.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.14.0"
  }
}
